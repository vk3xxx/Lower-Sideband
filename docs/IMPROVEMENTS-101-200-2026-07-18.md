# App Improvements 101–200 — 18 July 2026

This ledger records the second autonomous 100-improvement pass for the native Swift macOS and iOS Sideband applications.

## Portable conversation archives

101. Upgrade structured conversation archives to format version 2.
102. Preserve LXMF reply targets and reply quotes in archives.
103. Preserve reaction targets and reaction content in archives.
104. Preserve LXMF comment references in archives.
105. Preserve continuation references in archives.
106. Preserve interoperable command requests in archives.
107. Preserve attachment transfer state without exposing local paths.
108. Preserve delivery-attempt count and timestamp in archives.
109. Preserve delivery mode and bounded failure evidence in archives.
110. Retain legacy archive decoding while continuing to omit outbox ownership.

## Message bookmarks

111. Add durable starred-message state.
112. Timestamp star changes for deterministic cross-device resolution.
113. Decode legacy messages as unstarred safely.
114. Persist bookmarks inside encrypted application snapshots.
115. Merge the newest star or unstar action through CloudKit.
116. Preserve bookmark state in structured conversation exports.
117. Add per-conversation starred-message queries and global counts.
118. Add star and unstar actions to message context menus.
119. Add a starred-only conversation filter with visible star indicators.
120. Include bookmark state in accessibility labels and message diagnostics.

## Safe archive restoration

121. Add native structured-conversation archive import.
122. Bound archive input size before decoding.
123. Reject unsupported archive versions and invalid destinations.
124. Cap imported message and attachment resource use.
125. Deduplicate both local record IDs and LXMF message hashes.
126. Remap imported messages to the local canonical conversation ID.
127. Import no contact trust or fingerprint-verification authority.
128. Prevent historical queued or sent messages from being retransmitted.
129. Retain attachment metadata as unavailable without inventing payload data.
130. Add a native JSON file picker and clear import result reporting.

## Scheduled delivery

131. Add encrypted scheduled-delivery timestamps to outgoing messages.
132. Decode legacy messages with no schedule safely.
133. Preserve schedules in structured exports and CloudKit merges.
134. Validate schedules to a safe future window of at most one year.
135. Gate direct opportunistic delivery until a message is due.
136. Gate encrypted-link and attachment delivery until a message is due.
137. Gate propagation-node upload until a message is due.
138. Integrate the next due message with iOS background refresh scheduling.
139. Add native date selection and visible scheduled-state diagnostics.
140. Add send-now and cancel controls without weakening normal queue ownership.

## Notification privacy and actions

141. Add a per-contact notification-preview override.
142. Persist preview policy in encrypted conversation state.
143. Decode legacy contacts using the global preview policy.
144. Merge preview policy through CloudKit with the contact record.
145. Apply the resolved preview policy to text and reaction notifications.
146. Apply the resolved preview policy to attachment notifications.
147. Keep generic lock-screen text available for privacy-sensitive contacts.
148. Add a native notification quick-reply action.
149. Send quick replies to the correct conversation without changing selection.
150. Retain mark-read, deep-link, badge, sound, and notification cleanup behavior.

## Attachment storage health

151. Add encrypted attachment-store usage inspection.
152. Report logical payload size separately from encrypted disk size.
153. Detect missing referenced attachment files.
154. Detect corrupt or authentication-failing attachment files.
155. Detect unreferenced regular-file orphans without following symlinks.
156. Report orphan count and reclaimable byte size.
157. Add safe orphan-file cleanup.
158. Add failed attachment-metadata cleanup without deleting messages.
159. Add per-conversation attachment byte totals and copyable diagnostics.
160. Add native storage health, inspection, and cleanup controls.

## Adaptive gateway health

161. Persist success and failure history per public Reticulum endpoint.
162. Track consecutive failures independently from lifetime failures.
163. Place repeatedly failing endpoints into a bounded cooldown.
164. Clear endpoint cooldown immediately after a successful connection.
165. Maintain an exponentially smoothed connection-latency estimate.
166. Prefer healthy low-latency endpoints while retaining configured preference.
167. Prevent a preferred endpoint from overriding an active failure cooldown.
168. Keep cooled endpoints as last-resort fallbacks instead of removing them.
169. Add copyable gateway-health diagnostics.
170. Add a user-controlled health-history reset and deterministic ranking tests.

## Contact tags

171. Add up to eight encrypted tags per contact.
172. Trim, bound, case-insensitively deduplicate, and ignore empty tags.
173. Decode legacy conversations with an empty tag list.
174. Validate tag count and length during snapshot restoration.
175. Merge tags with the newest CloudKit contact record.
176. Transfer tags in version 2 contact collections.
177. Include tags in structured conversation archives.
178. Search conversations by partial tag text.
179. Add native comma-separated tag editing.
180. Show compact accessible tag summaries in conversation rows.

## Device-authentication app lock

181. Add an optional device-authentication privacy lock.
182. Support biometrics with device passcode fallback on macOS and iOS.
183. Start in a locked state when the persisted option is enabled.
184. Relock automatically whenever the application enters the background.
185. Render a content-free lock screen instead of conversation data.
186. Require successful authentication before enabling or disabling the lock.
187. Prevent cancelled or failed authentication from changing the setting.
188. Report authentication availability and safe failure descriptions.
189. Provide automatic and explicit unlock actions with progress state.
190. Persist only the lock preference, never biometric or passcode material.

## Repository quality and verification

191. Run the expanded native Swift regression suite with 194 tests.
192. Verify a complete unsigned macOS Debug application build.
193. Verify a complete unsigned iOS Simulator Debug application build.
194. Verify the manual-CI generic iOS device build command.
195. Audit Swift Package Manager for third-party runtime dependencies.
196. Audit application targets for Python runtime or library dependencies.
197. Scan tracked source and configuration for common secret signatures.
198. Scan tracked files for accidentally committed large binary artifacts.
199. Reconfirm GitHub Actions remains manual-dispatch only.
200. Add this exact versioned 101–200 implementation ledger and push every batch.

## Verification summary

- Native Swift tests: 194 passing before the final verification run.
- macOS Debug build: passing.
- iOS Simulator Debug build: passing.
- Runtime implementation: native Swift with Apple platform frameworks.
- CI trigger: manual `workflow_dispatch` only.
